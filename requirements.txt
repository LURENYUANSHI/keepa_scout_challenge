# Python 3.11+
# Core web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# DB
sqlalchemy>=2.0,<3.0
asyncpg>=0.29,<1.0

# HTTP client (Keepa, LLM providers that need raw calls)
httpx>=0.27,<1.0

# Config / validation
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0

# Background tasks
celery[redis]>=5.4,<6.0
redis>=5.0,<6.0

# Agent / LLM orchestration
langgraph>=0.2,<0.3
langgraph-checkpoint-postgres>=2.0,<3.0
langchain-openai>=0.1,<0.2

# langgraph-checkpoint-postgres depends on plain `psycopg` (not the binary
# wheel) -- on python:3.11-slim there's no system libpq, so a bare `psycopg`
# install fails to import at runtime ("no pq wrapper available"). Pinning
# the [binary] extra here pulls in the bundled C extension instead of
# needing `apt-get install libpq-dev` in the Dockerfile.
psycopg[binary]>=3.2,<4.0

# Auth
bcrypt>=4.1,<5.0

# Tests
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
respx>=0.21,<1.0

# `websockets` was already present transitively (uvicorn[standard] pulls it
# in for the ASGI server's own WS support), but WS /chat/stream
# (app/routers/chat.py) needs a real WS *client* too -- both
# starlette.testclient.TestClient (tests/test_chat_websocket.py) and any
# ad-hoc verification script talking to the running container use it
# directly, so it's pinned explicitly here instead of relying on it staying
# a transitive dependency of something else.
websockets>=13.0,<17.0
